package com.querybricks.condition;

import com.querybricks.QueryPart;

/**
 * Represents a conditional expression in a SQL query.
 * Conditions are parts of the query that evaluate to a boolean value,
 * typically used in WHERE, HAVING, or ON clauses.
 *
 * @since 1.0.0
 */
public interface Condition extends QueryPart {
}
